# 校园二手交易平台 · 执行计划

> 配套文档：[功能概述.md](file:///d:/study/project/campus-trade/功能概述.md)
>
> 本文档给出整个项目完善的执行顺序，每阶段含「目标 / 学什么 / 做什么 / 验证 / 产出」。Agent 相关阶段（C–G）为重点，写得较细。

## 阶段总览与依赖

```
A(地基清理) → B(工程规范化)
                ↓
        C(Python地基) → D(RAG检索) → E(工具+记忆) → F(导购Agent成型)
                                                       ↓
                                                 G(多agent+MCP)
                                                       ↓
                                                 H(推荐+收尾)
```

- **A→B 必须先做**：不清理地基就动 AI，技术债越滚越大。
- **B 与 C 可并行**：互不依赖，时间够可同时推进。
- **F 是分水岭**：做到 F 简历可写，G/H 为锦上添花。
- **H 放最后**：推荐复用 D 的向量，监控/测试需项目稳定才有意义。

### 当前进展（2026-08-22）

| 阶段 | 状态 | 说明 |
|---|---|---|
| A 地基清理 | ⏳ 待做 | .gitignore 已部分完善 |
| B 工程规范化 | ⏳ 待做 | WebSocket / Redis 待启用 |
| C Python 地基 | ✅ 完成 | FastAPI + 润色接口 + Java 转发链路通 |
| D RAG 检索 | ✅ 完成 | 本地 BGE ONNX + ChromaDB，检索/灌库/对话接口全通 |
| E 工具+记忆 | ⏳ 待做 | Function Calling + Redis 会话 |
| F 导购 Agent | 🔄 进行中 | 后端已通，差前端接入（见三 Agent 架构） |
| G 多 agent+MCP | ⏳ 待做 | 匹配 Agent + MCP |
| H 推荐+收尾 | ⏳ 待做 | |

**下一步重点**：把已完成的检索 Agent / 助手 Agent 接入前端。三 Agent 协作架构见 [功能概述.md 3.0 节](file:///d:/study/project/campus-trade/功能概述.md)。

### 测试账号（seed_products.sql 创建，密码统一 111111）

| student_no | phone | nickname | 人设 | role |
|---|---|---|---|---|
| 111111 | 111111 | 小李同学 | 数码控（已有） | STUDENT |
| 2024001 | 13800000001 | 考研张 | 考研党 | STUDENT |
| 2024002 | 13800000002 | 运动王 | 运动达人 | STUDENT |
| 2024003 | 13800000003 | 生活赵 | 生活家 | STUDENT |
| 2024004 | 13800000004 | 毕业陈 | 毕业清仓 | STUDENT |
| 2024005 | 13800000005 | 书虫孙 | 读书人 | STUDENT |

> 所有账号密码均为 `111111`，mode 均为 `student`。登录示例：`{"account":"2024001","password":"111111","mode":"student"}`

### 待优化清单（非阻塞，后期打磨）

| 优化项 | 现状 | 优化方向 | 优先级 |
|---|---|---|---|
| 检索 Rerank | BGE 向量检索 top2-5 区分度低（如"考研用平板"误召"滑板"） | 加 bge-reranker cross-encoder 精排 | 中 |
| 浏览量去重 | [ProductService.java#L240](file:///d:/study/project/campus-trade/backend/src/main/java/com/campus/product/service/ProductService.java#L240) 每次访问都 +1，同一用户刷新也累计 | 用 Redis 记录"用户 X 当天看过商品 Y"，按 UV 去重 | 低 |
| 商品图片 | 测试数据用 picsum.photos 占位图，不是真实商品图 | 上线后走 OSS 上传，或用按关键词取图的图库 | 低 |
| 灌库自动同步 | 商品发布/下架后要手动调 `/rag/index` 灌向量库 | 商品状态变更时自动触发灌库（增量同步） | 中 |

---

## 阶段 A：地基清理

**目标**：还技术债，让后续所有改动建立在干净工程上。

**做什么**
- 清理仓库里的 `BOOT-INF/`、`lib/`、编译产物，完善 `.gitignore`
- `application.yml` 敏感配置（数据库密码、API key、邮箱密码）全部走环境变量
- 统一异常处理与 `ApiResponse` 一致性核查
- 检查 Flyway 历史脚本是否被改动过（按 README 规范只追加不修改）

**验证**：`mvn clean package` 通过，仓库 `git status` 干净，无敏感信息提交。

**产出**：干净的可构建工程。

---

## 阶段 B：工程规范化补强

**目标**：把"已引入但没用"的技术债还掉。

**做什么**
1. **WebSocket 替换聊天轮询**：pom 已引入 `spring-boot-starter-websocket` 且有 `WebSocketController`，但 README 标注仍为前端轮询。核实后统一为真正的 WS 推送。
2. **Redis 真正启用**：
   - 验证码缓存（替代当前邮件直发逻辑）
   - 登录失败限流
   - 热门商品榜（基于浏览量 zset）
   - 接口缓存（分类列表等低频变更数据）
3. **测试骨架**：补 Controller/Service 单元测试与关键流程集成测试。

**验证**：聊天消息实时推送无轮询；Redis 中能查到缓存 key；`mvn test` 通过。

**产出**：WebSocket 化的聊天、生效的 Redis 缓存、基础测试覆盖。

---

## 阶段 C：Python AI 服务地基（AI 线起点 · 重点）

**目标**：跑通 Java ↔ Python 通信链路，消除"Python 怎么接进 Java 项目"的心理障碍。

### C.1 学习内容（够用即停）
- Python 虚拟环境：venv 或 conda 二选一，理解依赖隔离
- FastAPI 最小应用：路由、Pydantic 模型、`uvicorn` 启动
- httpx 调外部 API（异步 HTTP 客户端，比 requests 更现代）
- SSE（Server-Sent Events）流式响应原理：`StreamingResponse` + `text/event-stream`

### C.2 做什么
1. 在 `campus-trade/` 下新建 `ai-service/` 目录，结构：
   ```
   ai-service/
   ├── app/
   │   ├── main.py              # FastAPI 入口
   │   ├── api/
   │   │   └── polish.py        # 复刻润色接口
   │   ├── core/
   │   │   └── config.py        # 配置（环境变量读取）
   │   └── services/
   │       └── llm_client.py    # DeepSeek 调用封装
   ├── requirements.txt
   └── README.md
   ```
2. 用 FastAPI 复刻现有 [AiService.java](file:///d:/study/project/campus-trade/backend/src/main/java/com/campus/ai/service/AiService.java) 的润色逻辑（纯 LLM 调用，无 agent）。
3. Java 侧 [AiController.java](file:///d:/study/project/campus-trade/backend/src/main/java/com/campus/ai/controller/AiController.java) 的 `/api/ai/polish` 改为 HTTP 转发到 Python，**复用现有 JWT 鉴权**（Java 鉴权后透传用户 ID 给 Python，不重复鉴权）。
4. 前端行为保持不变。

### C.3 验证
- 前端调用润色功能，结果与之前一致
- Python 服务日志能看到请求
- Java 日志能看到转发调用
- 断开 Python 服务时 Java 返回友好降级提示

### C.4 产出
- 可独立运行的 Python AI 服务
- Java 转发层
- 跑通的混合架构基座

---

## 阶段 D：RAG 检索基础（✅ 已完成）

**目标**：实现商品语义检索，调好召回质量。**先不碰 LLM 生成，只做检索 → 返回列表**。

### D.1 学习内容
- **Embedding 本质**：把文本映射为高维向量，语义相近的文本向量距离近
- **向量相似度**：余弦相似度 / 点积，理解为什么用 cosine
- **Chroma 数据模型**：
  - `Collection`：类似数据库表，存一类向量
  - `Document`：原始文本
  - `Embedding`：向量
  - `Metadata`：结构化字段（如 `price`、`category`），支持过滤
  - `id`：唯一标识
- **召回质量评估**：人工标注一批 query，看 top-K 命中率

### D.2 做什么
1. 选定 Embedding 服务（智谱 / 通义千问，免费额度够用），封装 `embedding_client.py`。
2. 设计商品向量化内容：`标题 + 描述 + 分类 + 成色` 拼成文本，避免只向量化标题。
3. 写同步脚本 `sync_products.py`：
   - 从 MySQL 读商品（先全量，上架状态）
   - 调 Embedding API 向量化
   - 写入 Chroma，metadata 存 `price`、`category`、`seller_id` 等
4. 实现 `POST /agent/search`：
   - 入参：自然语言 query + 可选过滤条件
   - query 向量化 → Chroma 检索 top-K → 返回商品列表
5. 准备测试 query 集（考研平板 / 便宜显示器 / 200 自行车…），人工评估召回。

### D.3 验证（关键，决定后续 agent 质量）
- 用"考研用的平板"检索，结果是否含性能本/平板（语义理解）
- 用"便宜显示器"检索，是否返回低价显示器
- 对比关键词搜索，语义检索在模糊意图下是否更准
- top-K 命中率记录下来，作为基线

### D.4 产出
- 商品向量库（Chroma）
- 同步脚本
- `/agent/search` 接口
- 召回质量基线数据

---

## 阶段 E：Function Calling + Memory（重点）

**目标**：让 LLM 能自主调用 Java 接口作为工具，并支持多轮对话。

### E.1 学习内容
- **Tool Use / Function Calling 机制**：LLM 根据用户意图决定调哪个工具、传什么参数，工具返回结果后 LLM 基于结果生成回答
- **OpenAI 兼容 API 的 tools 格式**：`tools` 数组、`function_call` / `tool_calls` 字段
- **工具描述怎么写**：name / description / parameters (JSON Schema)，描述质量直接决定调用准确率
- **Memory 类型**：
  - 短期记忆：当前会话历史
  - 长期记忆：用户偏好（本阶段先不做）
  - Token 预算：历史过长要截断或摘要

### E.2 做什么
1. **封装 Java 工具接口**（Python 侧）：
   - `search_products(filters)` → 调 Java `/api/products`
   - `get_product_detail(id)` → 调 Java `/api/products/{id}`
   - `get_category_price_stats()` → 新增 Java 接口，返回分类均价（用于比价）
   - `get_user_history()` → 新增 Java 接口，返回用户浏览/收藏历史
2. **定义工具 schema**：每个工具写清楚 description 和 parameters，让 LLM 知道何时调。
3. **实现工具调用循环**：
   - LLM 返回 `tool_calls` → Python 执行对应工具 → 结果回灌 → LLM 继续生成
   - 支持多步调用（一次对话可调多个工具）
4. **Memory**：
   - Redis 存会话历史，key 为 `session_id`
   - 历史超阈值时截断老消息
   - 每轮把用户消息 + assistant 回复 + 工具调用记录都存入

### E.3 验证
- 用户："200 以内的自行车" → LLM 调 `search_products(maxPrice=200, keyword="自行车")` → 返回真实商品
- 用户："那个捷安特多少钱" → LLM 调 `get_product_detail` → 基于详情回答
- 连续 3 轮对话能正确继承上下文（"再便宜点的""换个颜色"）
- 工具调用日志可追溯（调了哪个工具、传了什么参数）

### E.4 产出
- 工具调用框架
- 4 个封装好的工具
- Redis 会话记忆
- 可多轮对话的 agent 雏形

---

## 阶段 F：导购 Agent 成型（LangGraph · 简历主卖点 · 最重点）

**目标**：用 LangGraph 把 D+E 重构为状态机式 Agent，完整跑通导购闭环。**这一步是简历主卖点成型**。

### F.1 为什么用 LangGraph 而非 LangChain
- LangChain 的 AgentExecutor 是黑盒，难以精细控制流程
- LangGraph 用**状态机 + 图**建模 agent，每个节点是一个处理步骤，边是条件转移
- 天然支持多 agent 协作（阶段 G 会用），状态可在 agent 间显式传递
- 简历写"基于 LangGraph 的多智能体编排"比"用了 LangChain"专业一档

### F.2 学习内容
- **StateGraph**：图的容器，定义状态结构（TypedDict）
- **Node（节点）**：一个函数，接收状态、返回状态更新
- **Edge（边）**：
  - 普通边：A 完成必去 B
  - 条件边：根据状态决定去哪（如路由到不同 agent）
- **Checkpointer**：状态持久化，支持中断恢复
- **ToolNode**：专门执行工具调用的内置节点

### F.3 导购 Agent 状态机设计

```
                    ┌─────────────┐
        用户消息 →  │ understand  │  理解意图，提取需求
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │  retrieve   │  RAG 语义检索（阶段 D 能力）
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │ tool_call   │  LLM 决定是否调工具（阶段 E 能力）
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │ tool_exec   │  执行工具，结果回灌
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │   reason    │  综合检索+工具结果，生成带理由推荐
                    └──────┬──────┘
                           ↓
                    ┌─────────────┐
                    │  respond    │  流式输出 + 更新 Memory
                    └─────────────┘
```

- `understand` / `reason` / `respond` 用 LLM 节点
- `retrieve` 调 Chroma
- `tool_call` → `tool_exec` 是 Function Calling 循环，可多轮
- 条件边：`tool_call` 后若 LLM 还要调工具则回 `tool_exec`，否则去 `reason`

### F.4 做什么
1. 定义 Agent 状态结构（`messages`、`retrieved_products`、`user_intent`、`tool_results`）
2. 实现各节点函数
3. 用 `StateGraph` 组装，加条件边
4. 接 Redis Checkpointer 实现会话状态持久化
5. **边界约束**（写在 system prompt + 工具白名单）：
   - 不替用户下单（不暴露下单工具）
   - 必须基于检索结果推荐，不编造商品
   - 不透露卖家隐私（工具返回时过滤敏感字段）
   - 不做议价（留给阶段 G 的议价 Agent）
6. 前端：
   - 商品页加"AI 找货"入口
   - 接 SSE 流式渲染（Python `StreamingResponse`）
   - 推荐卡片组件（商品图 + 标题 + 价格 + 推荐理由 + "联系卖家"按钮）

### F.5 验证（端到端场景）
1. "找辆 200 以内的自行车，最好校区自取" → 检索 + 工具协同 → 返回带理由推荐
2. "有没有适合考研用的 iPad，3000 预算" → 语义理解"考研用" → 推荐性能本/平板
3. "上次看中的那个键盘还在吗" → Memory + 工具 → 返回历史商品状态
4. "这个价位合理吗" → RAG 比价（同类均价） → 给出判断
5. 推荐卡片可点击跳转商品详情/联系卖家

### F.6 产出
- LangGraph 导购 Agent
- SSE 流式接口
- 前端 AI 找货入口与推荐卡片
- **简历主卖点成型**

---

## 阶段 G：多智能体协作 + MCP（AI 线进阶 · 重点）

**目标**：构建多 agent 协作闭环，封装 MCP Server。

### G.1 多智能体协作

**学习内容**
- 多 agent 状态传递：共享 State 还是消息传递
- Supervisor / Orchestrator 路由模式：一个调度 agent 决定把任务交给谁
- Handoff 机制：agent 之间如何移交控制权

**做什么**：加**议价 Agent**，构建"导购 → 议价"协作闭环。
- 议价 Agent 能力：参考同类均价（RAG）+ 双方信用评分，在合理区间协助协商
- LangGraph 编排：导购 Agent 完成推荐后，若用户表达议价意图，supervisor 路由到议价 Agent
- 议价 Agent 完成后结果回传，导购 Agent 整合输出

**协作链路示例**
```
用户："找辆 200 以内的自行车，帮我砍到 180"
   ↓
导购 Agent：检索 → 推荐"捷安特 200 元"
   ↓ (用户：帮我砍到 180)
Supervisor 路由 → 议价 Agent
   ↓
议价 Agent：查同类均价(180-220) + 卖家信用 → 建议出价区间
   ↓
返回协商建议
```

**验证**：上述链路端到端跑通，agent 间状态正确传递。

### G.2 MCP Server 封装

**学习内容**
- MCP（Model Context Protocol）协议：Anthropic 提的标准，"AI 世界的 USB 接口"
- Server / Tool / Resource 模型
- 为什么 MCP 比自定义工具接口更通用

**做什么**：把 Java 业务能力（查商品、查订单、发消息）封装为标准 MCP Server。
- 可在 Python 侧用 `mcp` 库实现，底层调 Java REST
- 也可在 Java 侧用社区 MCP SDK 实现

**验证**：用任意 MCP 客户端（如 Claude Desktop）能调到你的查商品能力。

### G.3 产出
- 多 agent 协作系统
- MCP Server
- 差异化加分项

---

## 阶段 H：推荐算法 + 工程化收尾

**目标**：补推荐算法，收口项目，准备投简历。

### H.1 推荐算法

**学习内容**
- 协同过滤原理（基于用户 / 基于物品）
- 内容召回（商品标签 + 向量相似度，**复用阶段 D 的向量**）
- 重排规则（信用、新鲜度、同城优先）
- 排序模型可选 LightGBM（进阶）

**做什么**
- 商品列表加个性化推荐
- 首页推荐与用户浏览/收藏历史相关
- 简历可写"协同过滤 + 内容召回 + 业务规则重排"

**验证**：首页推荐与用户行为相关，点击率统计。

### H.2 工程化收尾

- MongoDB 存 Agent 对话历史与非结构化日志
- 监控（接口耗时、agent 调用链、LLM token 消耗）
- 测试补全
- README 重写（含架构图、启动说明）
- 微服务化梳理（明确 Java / Python 边界）
- **简历话术与面试准备**（见 [功能概述.md](file:///d:/study/project/campus-trade/功能概述.md) 第六节）

### H.3 产出
- 个性化推荐
- 完整可投简历的项目
- 面试准备材料

---

## 关于《Hello Agent》怎么读

跳着读、对照阶段读，不要硬啃：
- 做阶段 D–E 时，只读 Embedding / RAG / Tool Use 章节
- 做阶段 F–G 时，再读 Agent 架构 / 多智能体章节
- 概念在动手用到时才真正懂，提前硬读必然糊涂

---

## 时间投入建议

- **A–B**：1–2 个学习单元（每个单元约半天到一天）
- **C**：1 个单元
- **D**：2 个单元（含召回调优）
- **E**：2 个单元
- **F**：3 个单元（最关键，不要赶）
- **G**：2–3 个单元
- **H**：2 个单元

主线 A→F 走通即可写简历，G/H 按时间投入决定深度。
